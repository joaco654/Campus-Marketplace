// Temporarily disabled NextAuth - using Supabase auth instead
// This prevents build-time database connection issues

export const authOptions = {
  // Placeholder - auth handled by Supabase client-side
}

